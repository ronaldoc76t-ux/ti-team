apiVersion: v1
kind: Service
metadata:
  name: cmdb-discovery
  namespace: ti-operations
  labels:
    app: cmdb-discovery
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: cmdb-discovery